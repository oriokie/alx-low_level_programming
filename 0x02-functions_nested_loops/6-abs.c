#include "main.h"
/**
* int_abs - Function computes the abosulte value of an integer
*@n: integer to be checked
* Return: Always 0
*/
int_abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
